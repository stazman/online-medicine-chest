***** TODOS:


Adds Resources feature

  __ Add resource from front end

        __ Sets up action/reducer

        __ Creates form

        __ Adds new resource to database

  __ Show all resources when navigate to resources page

    __ Shows new resource when new resource posted (ordered by date of entry)

  __ Delete resource


Adds Journals feature

Adds Advisor feature

Adds Community feature

  Adds posts and comments

Adds Tracker feature

  __ Add goals

  __ Update goals


***** Refactor:

Users

   __ Updates user

   __ Adds password rules to validation



***** Unused code/comments/for blog:


  //The first parameter passed to useState() is the state, here it's formData, for which we set its properties explicitly ('initial state' can we say ???); the second parameter, which sets the state when it changes, is shown here as setFormData, but it's value is not assigned until all of the properties are destructured into the setFormData variable/parameter.

// Without destructuring:
  // const ProfileInput = props => {

  // const profileHook = useState('');
  // const profileState = profileHook[0];
  // const setProfile = profileHook[1];
  // }






// import FoodAdvisorsContainer from '../containers/FoodAdvisorsContainer';
// import TrackerHome from './trackers/TrackerHome';
// import JournalHome from './journal/JournalHome';
// import JournalContainer from '../containers/JournalContainer';
// import ResourceHome from './resources/ResourceHome';
// import ResourcesContainer from '../containers/ResourcesContainer';
// import CommunityHome from './community/CommunityHome';

                {/* {
                  <Route
                    exact path='/tracker_home'
                    component={TrackerHome}
                  />
                } */}
                {/* {
                  <Route
                    exact path='/journal_home'
                    component={JournalHome}
                  />
                } */}
                {/* {
                  <Route
                    exact path='http://localhost:8000/resource_home'
                    component={ResourceHome}
                  />
                } */}
                {/* {
                  <Route
                    exact path='/community_home'
                    component={CommunityHome}
                  />
                } */}
              </Switch>

              {/* <FoodAdvisorsContainer />

              <JournalContainer />

              <ResourcesContainer /> */}
